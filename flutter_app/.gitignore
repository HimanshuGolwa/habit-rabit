# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# android/ is committed — it holds real config (app id, name, permissions).
# Its own android/.gitignore still excludes build artifacts, local.properties, keys.

# Generated platform folders we don't customize yet (CI regenerates web/).
ios/
web/
windows/
macos/
linux/

# IDE
.idea/
.vscode/
*.iml
